[project]
name = "ai-persona-backend"
version = "1.0.0"
description = "FastAPI backend for AI Persona voice and chat agent"
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
